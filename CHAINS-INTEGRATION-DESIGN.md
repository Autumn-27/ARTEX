# 反问思维链(chains)接入设计

> 状态:**已确认方向**(2026-09-12 与作者讨论定稿)。语料:`chains-20260701/chains/`(62 个 md、约 3MB、1,966 个场景节、9,577 个 Q/A 对,9 个类别前缀)。
> 前置阅读:`FIXPLAN-AND-EVALUATION.md` Part 2 需求三(语料分析);`AUDIT-REPORT.md`。
> 本文替代评估报告中"需求三"的初步建议(skills 化为主),以本文四层架构为准。

## 1. 接入意图(作者原意的确认)

chains 不是"一本可以查的书",而是"一套想问题的反射"。作者写它的动机:大知识库有两个死穴——体量装不进上下文,以及**检索动作本身依赖模型意识到自己需要查**;而渗透中最需要知识的时刻(卡住、被判据骗、单维度死磕)恰是模型最不自知的时刻。所以 chains 被蒸馏成"遇到 X 就先问自己 Y"的判据链,本质是**决策反射的触发器**。

因此接入形态的评价标准不是"能不能存下 3MB",而是:

1. **不靠模型自觉**——该出现时必须出现;
2. **在对的时刻出现**——SQLi 的链只在打 SQLi 时出现;
3. **成本固定可预期**——每轮开销有硬顶,不随语料增长。

## 2. 为什么不是"纯 skills 化"

skill 清单预算 8000 字符,9 个 skill 的数量本身没问题("限制 AI"的担忧在数量层面不成立)。真正的问题是机制:**skill 是 pull 模式**——模型要先想到"我该去查",链才进上下文。这恰是当初否掉大知识库的同一个病:走在错误判据上的模型不觉得需要查;`meta-stuck-detect` 针对的卡住场景里,"让模型主动调用卡住自检"在逻辑上是悖论。纯 skills 化 = 把知识库的病按比例缩小后保留。

## 3. 最终架构:拆成三种成分,四层形态

语料里混着三种性质不同的东西,各归各位:

| 层 | 内容 | 形态 | 触发方式 | 每轮成本 |
|---|---|---|---|---|
| **L1 监测** | 确定性判据:同质响应转向阈值(15~25 次,`meta-orthogonal.md`)、统一拦截层识别(同长度同状态码)、tarpit 固定延迟、200 伪装拦截字节级相同(`meta-stuck-detect.md`) | **Go 代码**(引擎/流量侧信号检测) | 引擎信号,100% 触发 | ~0 |
| **L2 纪律** | meta-\* 六篇蒸馏铁律(每篇 10~20 条,如"回显阴性从不等于漏洞阴性"),共 2~3KB | **planner system 常驻**;worker 只挂观测真伪/卡住相关少量条目 | 永远在场 | 固定 ~2-3K token |
| **L3 场景** | 技术链(web/ad/priv/app/pwn/recon/rev/pivot)蒸馏骨架 2~4KB/类,"场景→判据→转向" | **按意图条件注入 worker system**;planner 派意图时带 `chain_tags`(或引擎按意图文本/vulnclass 匹配),代码路由 | 代码路由,不靠模型 | 仅相关意图付 |
| **L4 全书** | 62 篇原文 | **1 个 chains skill + reference 文件**,模型在骨架指引下需要完整 Q&A 时自行 Read | 模型主动(pull,兜底) | 按需 |

要点:

- **L1 是 chains 相比普通知识库独有的价值**:很多判据是确定性信号,写在提示词里执行者是模型(随状态漂移),写在代码里执行者是平台(100% 触发)。流量全程过录制代理、activity 每步落库,信号源都现成。
- **L2 的蒸馏是一次性人工工作**,作者来做(哪些判据是灵魂只有作者知道),先示范 meta 两篇,照风格批量化。
- **L3 的路由用平台已有的结构化数据**:意图文本、vulnclass、资产类型,不需要模型判断"我现在需要哪条链"。worker system 本就 per-intent 重建(`agent/worker.go:388-407`),注入骨架不破坏额外缓存。
- **L4 才是 skill 的正确角色**:书,不是反射。注意 worker 无 Glob/Grep(`agent/worker.go:369`),SKILL.md 索引必须给精确文件路径。

## 4. "反问"作为工作流强制动作

Q&A 格式里 **Q 比 A 更有结构价值**——Q 是"此刻该问自己的问题"。在 worker 收尾/写回环节(现有 SDK Settlement 收尾机制,`agent/worker.go:144`)加一道固定程序:**本意图结束前,对照 L3 骨架里的 Q 清单自检一遍**。这让"反问"从知识变成工作流里的强制动作,"反问系统"名符其实。

延伸:需求二(验证 agent)的 verifier 提示词直接引用对应 vulnclass 的"判定"章节作为反证 checklist(见 `FIXPLAN-AND-EVALUATION.md` Part 2)。

## 5. 远期形态(记录,不在本期)

9,577 对"问题→带判据的回答"本身就是现成的 SFT 语料格式,真正的"内化"终点是微调;但绑死模型供应商,与平台 provider 可插拔设计冲突。先把四层跑起来,chains 作为蒸馏源与评估基准保留。

## 6. 实施步骤(待排期)

1. **ETL 解析脚本**:正则 `^### ` / `^\*\*Q(\d+)\*\*` 把 62 篇切成 `(category, file, section, qa[])` JSON,合成稳定 ID(现为节内重号);剥离/改写 `meta-close-the-kill.md` 中耦合其它平台(黑板 remember/recall、Ralph 压缩)的内容。
2. **meta 蒸馏**:作者示范 2 篇 → 批量化 → 进 planner/worker system(L2)。
3. **技术链骨架蒸馏 + chain_tags 路由**:意图增加 tags 字段或引擎侧匹配,worker 启动时拼入对应骨架(L3)。
4. **L1 代码判据**:盘点 activity/流量数据结构,确定同质响应、统一拦截等信号的检测挂点(引擎层),触发后注入提示或通知 planner。
5. **L4 skill 化**:1 个 chains skill,SKILL.md 写使用时机 + 62 篇精确路径索引。
6. **收尾自检**:Settlement 环节挂 Q 清单自检(第 4 节)。

## 7. 已排除的形态(记录理由)

- 全量静态注入:3MB ≈ 60~90 万 token,物理不可行。
- hint 通道:50 条上限、每轮全量进 planner 态势(`agent/tools.go:349`),容量不符。
- sidequestion 扩展:它是人发起的只读旁路、无工具、不回注主上下文(`sidequestion/service.go:11-14`),改造等于重造检索层。
- RAG/向量检索(kb_search):降级为可选增强;若 L3 路由命中效果不佳再考虑,优先级低于四层架构。
